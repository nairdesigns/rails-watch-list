<h1>My Movies</h1>
<div class="movies">
    <% @movies.each do |movie| %>
      <div class="movie">


        <% if movie.name == nil %>
          <% movie.name = "no name" %>
          <h2><%=  link_to movie.name, edit_movie_path(movie), class: "movie-link"  %></h2>

        <% else %>
          <h2></h2>
          <p></p>
          <p></p>
          <p></p>

<div class="card" style="width: 18rem;">
  <img class="card-img-top" src="  https://source.unsplash.com/random
" alt="Card image cap">
  <div class="card-body">
    <h5 class="card-title"><%=  link_to movie.name, edit_movie_path(movie), class: "movie-link"  %></h5>
    <p class="card-text"><%= movie.title %></p>
    <p class="card-text"><%= movie.overview %></p>
    <p class="card-text"><%= movie.rating %></p>
          <h2><%=  link_to movie.name, movie_path(movie), class: "movie-link"  %></h2>
  </div>
</div>
      </div>
        <% end %>

    <% end %>
  </div>
